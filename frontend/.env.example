VITE_API_URL=http://localhost:4000/api

# Optional: set your real GA4 Measurement ID (starts with "G-") to enable analytics.
# Leave blank/unset to disable tracking entirely — no script is loaded without it.
VITE_GA_MEASUREMENT_ID=
